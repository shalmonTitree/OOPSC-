#include <bits/stdc++.h>
using namespace std;

class Student {
    int age;

public:
    Student(int a) {
        age = a;
    }

    void show() {
        cout << "Age: " << age << endl;
    }
};

int main() {
    Student s(20);
    s.show();
    return 0;
}
